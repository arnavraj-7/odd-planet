# Contact form → Google Sheet.
# Apps Script Web App URL (ends in /exec) and the shared secret the script checks.
# See docs/google-sheet-webhook.md for how to produce these.
SHEETS_WEBHOOK_URL=
SHEETS_WEBHOOK_TOKEN=

# Admin panel at /admin. Unset means the panel stays locked for everyone.
ADMIN_PASSWORD=
